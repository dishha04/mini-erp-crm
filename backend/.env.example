PORT=5000
# placeholder, to be used once Prisma/Postgres is set up in Phase 2
DATABASE_URL=postgresql://username:password@host:port/database_name
# placeholder, to be used once auth is set up in Phase 3
JWT_SECRET=your_jwt_secret_here
NODE_ENV=development
